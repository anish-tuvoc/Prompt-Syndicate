// CurvedScreen is no longer used by seat layouts directly.
// Movie screen is embedded in MovieTheaterLayout.
// Sports pitch indicator is embedded in CurvedSeatLayout.
// Keeping this file as a re-exportable placeholder to avoid broken imports.

export function CurvedScreen(_props: { type?: string }) {
  return null;
}
